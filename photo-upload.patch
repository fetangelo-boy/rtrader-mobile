From 6a9f8099b46264ab88affe35585689bb2ea26c3c Mon Sep 17 00:00:00 2001
From: Claude <noreply@anthropic.com>
Date: Sun, 10 May 2026 13:32:54 +0000
Subject: [PATCH] feat: add photo upload functionality to chats

- Added image picker with expo-image-picker
- Implemented photo upload to Supabase Storage (receipts bucket)
- Added UI for image selection with preview before sending
- Updated sendMessage to support mediaUrl and mediaType
- Added loading states for image upload process
- Photos are displayed inline in chat messages

Supports:
- Pick image from device library
- Preview selected image before sending
- Upload to Supabase Storage
- Send message with photo
- Cancel photo selection before sending

https://claude.ai/code/session_01DVfVJ4gRwkfA5cjEt2KoWX
---
 app/chat/[id].tsx | 145 ++++++++++++++++++++++++++++++++++++++++++----
 1 file changed, 134 insertions(+), 11 deletions(-)

diff --git a/app/chat/[id].tsx b/app/chat/[id].tsx
index 3699e5c..5bd6770 100644
--- a/app/chat/[id].tsx
+++ b/app/chat/[id].tsx
@@ -11,6 +11,7 @@ import {
   Keyboard,
   Image,
 } from "react-native";
+import * as ImagePicker from "expo-image-picker";
 import { ScreenContainer } from "@/components/screen-container";
 import { useColors } from "@/hooks/use-colors";
 import { useLocalSearchParams, useRouter } from "expo-router";
@@ -18,6 +19,7 @@ import { useState, useRef, useEffect } from "react";
 import { cn } from "@/lib/utils";
 import { trpc } from "@/lib/trpc";
 import { supabase } from "@/lib/supabase-client";
+import { v4 as uuidv4 } from "uuid";
 
 interface Message {
   id: string;
@@ -54,6 +56,8 @@ export default function ChatDetailScreen() {
   const [newMessage, setNewMessage] = useState("");
   const [replyingTo, setReplyingTo] = useState<Message | null>(null);
   const [loading, setLoading] = useState(true);
+  const [selectedImage, setSelectedImage] = useState<ImagePicker.ImagePickerAsset | null>(null);
+  const [uploadingImage, setUploadingImage] = useState(false);
   const scrollViewRef = useRef<FlatList>(null);
   const inputRef = useRef<TextInput>(null);
 
@@ -141,10 +145,13 @@ export default function ChatDetailScreen() {
           ? { author: replyingTo.author, content: replyingTo.content }
           : undefined,
         isOwn: true,
+        media_type: sentMessage.mediaType || null,
+        media_url: sentMessage.mediaUrl || null,
       };
       setMessages((prev) => [...prev, newMsg]);
       setNewMessage("");
       setReplyingTo(null);
+      setSelectedImage(null);
       Keyboard.dismiss();
       setTimeout(() => {
         scrollViewRef.current?.scrollToEnd({ animated: true });
@@ -198,12 +205,28 @@ export default function ChatDetailScreen() {
     }
   }, [messagesError]);
 
-  const handleSendMessage = () => {
-    sendMessageMutation.mutate({
-      chatId: String(chatId),
-      content: newMessage.trim(),
-      replyToId: replyingTo?.id ? String(replyingTo.id) : undefined,
-    });
+  const handleSendMessage = async () => {
+    // If image is selected, upload it first
+    if (selectedImage) {
+      const mediaUrl = await uploadImageToSupabase(selectedImage);
+      if (!mediaUrl) return;
+
+      sendMessageMutation.mutate({
+        chatId: String(chatId),
+        content: newMessage.trim() || "📷 фото",
+        replyToId: replyingTo?.id ? String(replyingTo.id) : undefined,
+        mediaUrl: mediaUrl,
+        mediaType: "image",
+      });
+
+      setSelectedImage(null);
+    } else {
+      sendMessageMutation.mutate({
+        chatId: String(chatId),
+        content: newMessage.trim(),
+        replyToId: replyingTo?.id ? String(replyingTo.id) : undefined,
+      });
+    }
   };
 
   const handleReply = (message: Message) => {
@@ -211,6 +234,65 @@ export default function ChatDetailScreen() {
     inputRef.current?.focus();
   };
 
+  const handlePickImage = async () => {
+    try {
+      const result = await ImagePicker.launchImageLibraryAsync({
+        mediaTypes: ImagePicker.MediaTypeOptions.Images,
+        allowsEditing: true,
+        aspect: [4, 3],
+        quality: 0.8,
+      });
+
+      if (!result.canceled && result.assets[0]) {
+        setSelectedImage(result.assets[0]);
+      }
+    } catch (error) {
+      console.error("Error picking image:", error);
+      Alert.alert("Ошибка", "Не удалось выбрать изображение");
+    }
+  };
+
+  const uploadImageToSupabase = async (asset: ImagePicker.ImagePickerAsset): Promise<string | null> => {
+    try {
+      setUploadingImage(true);
+
+      // Generate unique filename
+      const fileExt = asset.uri.split('.').pop() || 'jpg';
+      const fileName = `${uuidv4()}.${fileExt}`;
+      const filePath = `chats/${chatId}/${fileName}`;
+
+      // Fetch image blob
+      const response = await fetch(asset.uri);
+      const blob = await response.blob();
+
+      // Try to upload to receipts bucket (fallback: use same bucket for all uploads)
+      const bucketName = 'receipts';
+      const { error: uploadError } = await supabase.storage
+        .from(bucketName)
+        .upload(filePath, blob, {
+          contentType: 'image/jpeg',
+          upsert: false,
+        });
+
+      if (uploadError) {
+        throw uploadError;
+      }
+
+      // Get public URL
+      const { data: publicData } = supabase.storage
+        .from(bucketName)
+        .getPublicUrl(filePath);
+
+      return publicData.publicUrl;
+    } catch (error) {
+      console.error("Error uploading image:", error);
+      Alert.alert("Ошибка", "Не удалось загрузить изображение. Проверьте интернет соединение.");
+      return null;
+    } finally {
+      setUploadingImage(false);
+    }
+  };
+
   const renderMessage = ({ item }: { item: Message }) => (
     <Pressable
       onLongPress={() => handleReply(item)}
@@ -335,6 +417,33 @@ export default function ChatDetailScreen() {
           </View>
         )}
 
+        {selectedImage && (
+          <View
+            className="px-4 py-2 border-b flex-row items-center justify-between"
+            style={{ borderBottomColor: colors.border, backgroundColor: colors.surface }}
+          >
+            <View className="flex-1 flex-row items-center gap-2">
+              <Image
+                source={{ uri: selectedImage.uri }}
+                style={{ width: 40, height: 40, borderRadius: 4 }}
+                resizeMode="cover"
+              />
+              <View className="flex-1">
+                <Text className="text-xs text-muted">Выбранное фото</Text>
+                <Text className="text-sm text-foreground" numberOfLines={1}>
+                  {selectedImage.fileName || "Изображение"}
+                </Text>
+              </View>
+            </View>
+            <Pressable
+              onPress={() => setSelectedImage(null)}
+              style={({ pressed }) => [{ opacity: pressed ? 0.6 : 1, padding: 8 }]}
+            >
+              <Text className="text-lg text-foreground">✕</Text>
+            </Pressable>
+          </View>
+        )}
+
         {replyingTo && (
           <View
             className="px-4 py-2 border-b flex-row items-center justify-between"
@@ -369,6 +478,20 @@ export default function ChatDetailScreen() {
             className="px-4 py-3 border-t flex-row items-center gap-2"
             style={{ borderTopColor: colors.border }}
           >
+            <Pressable
+              onPress={handlePickImage}
+              disabled={uploadingImage || sendMessageMutation.isPending}
+              style={({ pressed }) => [
+                {
+                  opacity: uploadingImage || sendMessageMutation.isPending ? 0.3 : pressed ? 0.7 : 1,
+                  padding: 8,
+                },
+              ]}
+            >
+              <Text className="text-xl">
+                {uploadingImage ? "⏳" : "📷"}
+              </Text>
+            </Pressable>
             <TextInput
               ref={inputRef}
               value={newMessage}
@@ -380,20 +503,20 @@ export default function ChatDetailScreen() {
               multiline={true}
               returnKeyType="send"
               onSubmitEditing={() => {
-                if (newMessage.trim()) {
+                if (newMessage.trim() || selectedImage) {
                   handleSendMessage();
                 }
               }}
               blurOnSubmit={true}
-              editable={!sendMessageMutation.isPending}
+              editable={!sendMessageMutation.isPending && !uploadingImage}
             />
             <Pressable
               onPress={handleSendMessage}
-              disabled={!newMessage.trim() || sendMessageMutation.isPending}
+              disabled={(!newMessage.trim() && !selectedImage) || sendMessageMutation.isPending || uploadingImage}
               style={({ pressed }) => [
                 {
                   opacity:
-                    !newMessage.trim() || sendMessageMutation.isPending
+                    (!newMessage.trim() && !selectedImage) || sendMessageMutation.isPending || uploadingImage
                       ? 0.3
                       : pressed
                       ? 0.7
@@ -403,7 +526,7 @@ export default function ChatDetailScreen() {
               ]}
             >
               <Text className="text-xl">
-                {sendMessageMutation.isPending ? "⏳" : "✈️"}
+                {sendMessageMutation.isPending || uploadingImage ? "⏳" : "✈️"}
               </Text>
             </Pressable>
           </View>
-- 
2.43.0

